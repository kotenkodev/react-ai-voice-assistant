[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "python-decouple",
    "python-dotenv",
]

[tool.pyright]
venvPath = "."
venv = "venv"
