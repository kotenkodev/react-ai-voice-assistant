[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "python-decouple",
    "python-dotenv",
    "requests>=2.34.2",
]

[tool.pyright]
venvPath = "."
venv = "venv"
